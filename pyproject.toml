[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pathfinderai"
version = "0.1.0"
description = "An AI-powered cycling route recommendation engine"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["src"]
